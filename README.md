# BCX19 – Deploy InfluxDB

Either copy & paste everything into the Web UI (Import JSON/YAML).

Or import using the command line (you can replace `oc` with `kubectl`):

    oc apply -f . # imports all *.yaml files

